name: road_map
description: A premium, responsive, and highly customizable generic tournament bracket (road map) library for Flutter, supporting dynamic infinite rounds and premium animations.
version: 1.0.0
homepage: https://github.com/truonglam101191-creator/road_map_matches
repository: https://github.com/truonglam101191-creator/road_map_matches

environment:
  sdk: '>=3.11.4 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
